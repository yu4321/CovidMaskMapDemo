import { AppOffsetTopDirective } from './app-offset-top.directive';

describe('AppOffsetTopDirective', () => {
  it('should create an instance', () => {
    const directive = new AppOffsetTopDirective(null);
    expect(directive).toBeTruthy();
  });
});
