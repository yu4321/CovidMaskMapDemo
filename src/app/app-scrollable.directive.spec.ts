import { AppScrollableDirective } from './app-scrollable.directive';

describe('AppScrollableDirective', () => {
  it('should create an instance', () => {
    const directive = new AppScrollableDirective(null);
    expect(directive).toBeTruthy();
  });
});
